{
  "name": "prism",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro",
    "prebuild": "node -e \"require('node:fs').copyFileSync('src/styles/tokens.css','public/tokens-preview.css')\"",
    "predev": "node -e \"require('node:fs').copyFileSync('src/styles/tokens.css','public/tokens-preview.css')\""
  },
  "dependencies": {
    "astro": "^7.3.5"
  },
  "allowScripts": {
    "esbuild": true
  }
}