---
const { count, total } = Astro.props;
---
<section class="hero" aria-labelledby="hero-title">
  <div class="hero__field" aria-hidden="true"></div>

  <figure class="hero__plate">
    <svg viewBox="0 0 900 470" role="img" aria-label="Plan view of a variable-geometry fighter, wings sweeping from forward to aft">
      <g class="rules" aria-hidden="true">
        <line x1="450" y1="8" x2="450" y2="462" />
        <line x1="30" y1="252" x2="870" y2="252" />
        <circle cx="450" cy="252" r="204" />
        <circle cx="450" cy="252" r="126" />
      </g>

      <!-- wing gloves: the fixed root the sweeping panels pivot inside -->
      <g class="craft">
        <path class="ln glove" d="M472 206 L560 236 L566 262 L472 272 Z" />
        <path class="ln glove" d="M428 206 L340 236 L334 262 L428 272 Z" />

        <!-- sweeping panels, pivoting at the glove -->
        <g class="wing wing--stbd">
          <path class="ln" d="M556 234 L806 262 L826 276 L810 284 L560 276 Z" />
          <path class="ln slat" d="M566 240 L792 264" />
        </g>
        <g class="wing wing--port">
          <path class="ln" d="M344 234 L94 262 L74 276 L90 284 L340 276 Z" />
          <path class="ln slat" d="M334 240 L108 264" />
        </g>

        <!-- fuselage: long nose, flat pancake between the nacelles -->
        <path class="ln hull" d="M450 22
          C459 46 464 78 466 112 L469 160 L471 200
          L472 236 L474 286 L476 330
          L470 356 L466 392 L458 414 L452 438 L450 452
          L448 438 L442 414 L434 392 L430 356 L424 330
          L426 286 L428 236 L429 200 L431 160 L434 112
          C436 78 441 46 450 22 Z" />

        <!-- engine nacelles, set wide -->
        <path class="ln nacelle" d="M478 240 L512 250 L516 330 L508 392 L490 400 L476 330 Z" />
        <path class="ln nacelle" d="M422 240 L388 250 L384 330 L392 392 L410 400 L424 330 Z" />

        <!-- twin canted stabilisers -->
        <path class="ln fin" d="M486 300 L534 232 L548 238 L512 318 Z" />
        <path class="ln fin" d="M414 300 L366 232 L352 238 L388 318 Z" />

        <!-- stabilators -->
        <path class="ln stab" d="M492 356 L590 388 L600 400 L574 402 L490 378 Z" />
        <path class="ln stab" d="M408 356 L310 388 L300 400 L326 402 L410 378 Z" />

        <!-- nozzles -->
        <path class="ln nozzle" d="M482 392 L506 392 L500 416 L484 414 Z" />
        <path class="ln nozzle" d="M418 392 L394 392 L400 416 L416 414 Z" />

        <!-- tandem canopy -->
        <path class="ln canopy" d="M450 50 C461 62 465 88 464 120 L450 128 L436 120 C435 88 439 62 450 50 Z" />
        <path class="ln detail" d="M450 88 L450 128 M450 128 L450 200 M450 286 L450 392" />
        <path class="ln detail" d="M472 236 L560 250 M428 236 L340 250" />
        <path class="ln detail" d="M466 112 L472 206 M434 112 L428 206" />
      </g>

      <g class="marks" aria-hidden="true">
        <path d="M18 18 h22 M18 18 v22 M882 18 h-22 M882 18 v22
                 M18 452 h22 M18 452 v-22 M882 452 h-22 M882 452 v-22" />
      </g>

      <g class="sweepline" aria-hidden="true">
        <text x="450" y="466" text-anchor="middle" class="sweepline__t">sweep 20° — 68°</text>
      </g>
    </svg>
  </figure>

  <div class="hero__say">
    <p class="badge"><span class="badge__dot"></span>plate 01 · variable geometry</p>
    <h1 id="hero-title">
      <span class="ln1"><i>Components</i></span>
      <span class="ln2"><i>built to a</i></span>
      <span class="ln3"><i>tolerance.</i></span>
    </h1>
    <p class="hero__lead">
      Thirty pieces over ninety days. Plain HTML, CSS and JavaScript, every one recorded
      with the prompt that produced it, and not a single colour a machine did not derive.
    </p>
    <dl class="hero__spec">
      <div><dt>airframes</dt><dd>{count} / {total}</dd></div>
      <div><dt>payload</dt><dd>0 deps</dd></div>
      <div><dt>tolerance</dt><dd>literal colour fails the build</dd></div>
    </dl>
  </div>
</section>

<style>
  .hero {
    position: relative; isolation: isolate;
    display: grid; grid-template-columns: 1.1fr 1fr; gap: var(--s-l);
    align-items: center; padding: var(--s-m) 0 var(--s-2xl);
  }

  .hero__field {
    position: absolute; inset: -10% -20% 0; z-index: -1;
    background:
      radial-gradient(52% 44% at 30% 42%, color-mix(in oklab, var(--accent) 16%, transparent), transparent 70%),
      repeating-linear-gradient(to right, var(--border) 0 1px, transparent 1px 72px),
      repeating-linear-gradient(to bottom, var(--border) 0 1px, transparent 1px 72px);
    mask-image: radial-gradient(76% 62% at 32% 46%, #000 8%, transparent 74%);
  }

  .hero__plate { margin: 0; }
  .hero__plate svg { width: 100%; height: auto; overflow: visible; }

  .rules line, .rules circle { stroke: var(--border); stroke-width: 1; fill: none; }
  .rules circle { stroke-dasharray: 2 9; }

  .ln { fill: none; stroke: var(--text); stroke-width: 1.3; stroke-linejoin: round; stroke-linecap: round; }
  .hull    { fill: color-mix(in oklab, var(--surface) 92%, transparent); stroke-width: 1.6; }
  .glove,
  .nacelle { fill: color-mix(in oklab, var(--surface) 70%, transparent); }
  .wing .ln { fill: color-mix(in oklab, var(--surface) 55%, transparent); }
  .slat    { stroke: color-mix(in oklab, var(--accent) 55%, transparent); stroke-width: .9; fill: none; }
  .fin     { fill: color-mix(in oklab, var(--accent) 18%, transparent); stroke: color-mix(in oklab, var(--accent) 75%, var(--text)); }
  .stab    { fill: color-mix(in oklab, var(--surface) 62%, transparent); }
  .nozzle  { fill: var(--accent); stroke: var(--accent); }
  .canopy  { fill: color-mix(in oklab, var(--accent) 26%, transparent); stroke: color-mix(in oklab, var(--accent) 85%, var(--text)); }
  .detail  { stroke: color-mix(in oklab, var(--text) 42%, transparent); stroke-width: .85; }
  .marks path { stroke: var(--accent); stroke-width: 1.4; fill: none; }
  .sweepline__t { fill: var(--text-dim); font-family: var(--mono); font-size: 11px; letter-spacing: .06em; }

  /* The wings pivot at their glove. Sweep is the aircraft's defining trait,
     so it is the animation rather than decoration on top of one. */
  .wing--stbd { transform-origin: 560px 252px; }
  .wing--port { transform-origin: 340px 252px; }

  .hero__say { display: grid; gap: var(--s-2xs); }

  .badge {
    justify-self: start; display: inline-flex; align-items: center; gap: 7px;
    padding: 5px 13px; border-radius: 999px;
    background: color-mix(in oklab, var(--accent) 13%, transparent);
    color: var(--accent); font-family: var(--mono); font-size: var(--f-n2);
  }
  .badge__dot { width: 6px; height: 6px; border-radius: 50%; background: var(--accent); }

  .hero__say h1 { font-size: var(--f-6); line-height: .94; letter-spacing: var(--tr-display); }
  .hero__say h1 span { display: block; overflow: hidden; }
  .hero__say h1 i { display: block; font-style: normal; }
  .ln3 i { color: var(--accent); }

  .hero__lead { margin-top: var(--s-2xs); font-size: var(--f-1); line-height: var(--lh-lead); color: var(--text-dim); max-width: 40ch; }

  .hero__spec { display: flex; flex-wrap: wrap; gap: 6px; margin-top: var(--s-s); }
  .hero__spec > div {
    padding: 8px 14px; border-radius: var(--radius-sm);
    background: color-mix(in oklab, var(--surface) 62%, transparent); border: var(--rule);
  }
  .hero__spec dt { font-family: var(--mono); font-size: var(--f-n2); color: var(--text-dim); }
  .hero__spec dd { margin: 3px 0 0; font-size: var(--f-n1); }

  @media (prefers-reduced-motion: no-preference) {
    .rules line, .rules circle { opacity: 0; animation: fade .5s ease forwards .05s; }

    .ln {
      stroke-dasharray: 1200; stroke-dashoffset: 1200; fill-opacity: 0;
      animation: draw 1.1s cubic-bezier(.6,0,.3,1) forwards, fill .6s ease forwards;
    }
    .hull    { animation-delay: .20s, 1.05s }
    .glove   { animation-delay: .40s, 1.18s }
    .nacelle { animation-delay: .50s, 1.26s }
    .wing .ln{ animation-delay: .62s, 1.36s }
    .fin     { animation-delay: .74s, 1.44s }
    .stab    { animation-delay: .82s, 1.52s }
    .nozzle  { animation-delay: .90s, 1.60s }
    .canopy  { animation-delay: .98s, 1.68s }
    .detail  { animation-delay: 1.10s, 1.76s; stroke-dasharray: 340; stroke-dashoffset: 340; }

    /* once drawn, the wings sweep aft and settle */
    .wing--stbd { animation: sweep-stbd 2.6s cubic-bezier(.5,0,.2,1) 1.7s both; }
    .wing--port { animation: sweep-port 2.6s cubic-bezier(.5,0,.2,1) 1.7s both; }
    .sweepline__t { opacity: 0; animation: fade .6s ease forwards 2.1s; }
    .marks path { opacity: 0; animation: fade .5s ease forwards 1.9s; }

    .hero__say h1 i { translate: 0 110%; animation: lift .85s cubic-bezier(.16,1,.3,1) forwards; }
    .ln1 i { animation-delay: .30s } .ln2 i { animation-delay: .40s } .ln3 i { animation-delay: .50s }
    .badge, .hero__lead, .hero__spec { opacity: 0; animation: rise .6s cubic-bezier(.2,.7,.3,1) forwards; }
    .badge { animation-delay: .18s } .hero__lead { animation-delay: .70s } .hero__spec { animation-delay: .82s }
    .badge__dot { animation: pulse 2.4s ease-in-out infinite 2.6s; }
  }

  @keyframes draw { to { stroke-dashoffset: 0 } }
  @keyframes fill { to { fill-opacity: 1 } }
  @keyframes fade { to { opacity: 1 } }
  @keyframes lift { to { translate: 0 0 } }
  @keyframes rise { from { opacity: 0; translate: 0 12px } to { opacity: 1; translate: 0 0 } }
  @keyframes pulse { 0%, 100% { opacity: 1 } 50% { opacity: .3 } }
  @keyframes sweep-stbd { 0% { rotate: 0deg } 45% { rotate: -9deg } 100% { rotate: 26deg } }
  @keyframes sweep-port { 0% { rotate: 0deg } 45% { rotate: 9deg } 100% { rotate: -26deg } }

  @media (max-width: 880px) {
    .hero { grid-template-columns: 1fr; gap: var(--s-m); }
    .hero__plate { max-width: 520px; margin: 0 auto; }
  }
</style>
